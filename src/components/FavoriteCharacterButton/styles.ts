import {StyleSheet} from 'react-native';

export const FavoriteCharacterButtonStyle = StyleSheet.create({
  button: {
    marginHorizontal: 8,
    marginTop: 12,
    height: 30,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 6,
  },
});
